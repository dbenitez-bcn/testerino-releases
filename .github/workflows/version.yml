---
name: "tagged-release"

on:
  push:
    branches: [ "develop" ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Clone repository
      uses: actions/checkout@v4
    - name: Fuck
      run: VERSION_NUMBER=$(yq '.version' ./version.yml)
    - name: Show version!
      run: echo $VERSION_NUMBER