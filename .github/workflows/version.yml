---
name: "tagged-release"

on:
  push:
    branches: [ "develop" ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Version
      run: yq '.version' version.yml >> $VERSION_NUMBER
    - name: Show version!
      run: echo $VERSION_NUMBER